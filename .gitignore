# --- IDE & OS (File rác hệ thống) ---
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db

# --- BACKEND (Spring Boot / Java) ---
target/
*.class
*.jar
*.war
.mvn/wrapper/maven-wrapper.jar
# Nếu bạn dùng file log
*.log

# --- FRONTEND (NodeJS / React) ---
node_modules/
dist/
build/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnp.*

# --- SECURITY (QUAN TRỌNG NHẤT) ---
# Chặn các file chứa mật khẩu, API Key, JWT Secret
.env
.env.local
.env.*.local

# Nếu file properties chứa pass thật thì chặn luôn
# Chỉ nên up application.properties mẫu, còn dev/prod thì chặn
application-dev.properties
application-prod.properties

# --- DATABASE ---
# Nếu bạn lỡ tạo DB sqlite hoặc file backup
*.sql
*.sqlite
*.db